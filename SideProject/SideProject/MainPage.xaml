<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SideProject"
             x:Class="SideProject.MainPage"
             BindingContext="{Binding ContactInformationViewModel, Source={StaticResource ViewModelLocator}}">

    <!--<ContentPage.BindingContext>
        <local:ViewModels.ContactInformationViewModel/>
    </ContentPage.BindingContext>-->
    <StackLayout>
        <Label Text="{Binding Name}" FontSize="35" />
        <Label Text="{Binding Counter}" FontSize="18" />
        <Entry Text="{Binding NameInput}" Keyboard="Numeric" FontSize="15" />
        <Button Text="Incrementar contador" Command="{Binding IncreaseCounterCommand}"></Button>
    </StackLayout>


</ContentPage>
